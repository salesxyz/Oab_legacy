{
  "name": "oab-mentoria",
  "private": true,
  "version": "1.0.0",
  "description": "OAB Mentoria — monorepo (backend + web). Um comando para instalar, configurar e rodar tudo.",
  "workspaces": [
    "backend",
    "web"
  ],
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "concurrently -n backend,web -c blue,magenta \"npm run dev --workspace=backend\" \"npm run dev --workspace=web\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=backend && npm run build --workspace=web",
    "test": "npm run test --workspace=backend",
    "db:migrate": "npm run db:migrate --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
